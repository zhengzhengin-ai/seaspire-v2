.card {
  background: #ffffff;
  border: 1px solid #eef3f6;
  border-radius: 18px;
  overflow: hidden;
  box-shadow: 0 10px 30px rgba(0, 0, 0, .06);
  transition:
    transform .35s ease,
    box-shadow .35s ease,
    border-color .35s ease;
}

.card:hover {
  transform: translateY(-6px);
  border-color: #0E7490;
  box-shadow: 0 18px 42px rgba(0, 91, 150, .12);
}

.imageWrapper {
  position: relative;
  overflow: hidden;
  aspect-ratio: 4 / 3;
}

.image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  display: block;
  transition: transform .45s ease;
}

.card:hover .image {
  transform: scale(1.05);
}

.content {
  padding: 32px;
}

.content h3 {
  color: #005B96;
  font-size: 26px;
  font-weight: 600;
  margin-bottom: 16px;
  transition: .3s;
}

.card:hover h3 {
  color: #14B8A6;
}

.content p {
  color: #475569;
  font-size: 16px;
  line-height: 1.9;
  margin-bottom: 24px;
}

.link {
  color: #005B96;
  text-decoration: none;
  font-weight: 600;
  transition: color .3s;
}

.link:hover {
  color: #0E7490;
}

@media (max-width: 768px) {
  
  .content {
    padding: 26px;
  }

  .content h3 {
    font-size: 22px;
  }

  .content p {
    font-size: 15px;
  }
}